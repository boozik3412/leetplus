# Спецификация: операционный дашборд ассортимента

## Задача и решение

Владелец сети должен видеть продажи и состояние запасов в одном рабочем дашборде, понимать ограничения данных и открывать конкретные товары для действия. Исправляем действующий LeetPlus без смены дизайна, auth-контуров, платформы или хостинга. Предшествующий аудит подтвердил старые остатки, неверный источник цены и отсутствующую club-binding сессий.

## Пользовательские истории и приёмка

| ID | История и проверяемый результат |
|---|---|
| R01 | Остатки обновляются автономным INTERNAL daily worker отдельно от QUICK продаж. Новый snapshot хранит фактическую дату снимка, исторический запуск не выдаёт текущие goods за исторический запас. Provider failure остаётся наблюдаемым; повтор идемпотентен. |
| R02 | Свежесть каждого раздела оценивается по его фактам и club/source scope. Успех QUICK другого раздела не освежает inventory; одна свежая точка не маскирует устаревшую. Порог36ч остаётся существующим. |
| R03 | Импорт связывает сессию только с доказанным tenant/provider/domain/club. При отсутствии clubId разрешён единственный active Store данного domain, при нескольких сохраняется unknown. Existing historical sessions могут использовать тот же доказуемый read-time mapping без reward/event replay; unresolved coverage видна, не подменяется нулём. Проверяются network,1club,multiclub,cross-tenant. |
| R04 | Цена выбирается по точной club/product configuration либо подтверждённым sales фактам, с указанием источника и покрытия; неизвестная цена даёт null, не0. Количественное действие пополнения не зависит от доступности рублёвой оценки. |
| R05 | OOS строится одним engine по товару×клубу: текущий подтверждённый нулевой/отрицательный остаток и спрос; low-stock — положительный остаток до3дней. Missing snapshot отдельно, одинаковые exclusions и demand21d для карточек и отчётов. Сетевой запас не маскирует дефицит другого клуба. |
| R06 | Постоянная карточка «Нет в наличии» показывает число или точную причину недоступности, дату данных и открывает соответствующий список. |
| R07 | Постоянная карточка «Закончится за3дня» считает только положительный stock с подтверждённым спросом и открывает тот же список, что учтён в числе. |
| R08 | Карточка «Без продаж» переключает7/14/21/30дней, выбор отражён вURL. По умолчанию21. Положительный stock и отсутствие sales за окно; новые поступления исключаются по согласованной существующей логике. Не называть это отсутствием всех складских движений. |
| R09 | «Деньги в этих остатках» оценивает тот же no-sales набор; при неполной цене показывает подтверждённую часть и процент покрытия. Не смешивать стоимость stock с выручкой/прибылью за период в один псевдоэффект. |
| R10 | Ниже отображаются дни запаса/оборачиваемость по данным продаж и наличию. Нулевой спрос даёт «Нет продаж» или null, не бесконечность/NaN. Формула и период доступны. |
| R11 | Избыточный запас выделяется отдельным числом/стоимостью с объяснённым порогом: сохраняем действующий порог existing turnover отчёта, если он есть; иначе7дневная потребность применяется только для рекомендуемого заказа, избыток — явно30дней. Список и карточка согласованы. |
| R12 | Списания показывают сумму и количество за выбранный период, ведут к тем же движениям с существующими фильтрами. Если источник движений не подтверждён, отсутствие строк не объявляется подтверждённым0. |
| R13 | Период, массив клубов, массив категорий и окно no-sales переносятся из карточек в существующие отчёты. URL-параметры переводятся явным адаптером; permissions/fresh scope остаются серверными. Нельзя сохранять только первый выбранный клуб. |
| R14 | У каждой недоступной метрики есть короткая русская причина и уместное действие. Failed,stale,missing,partial различаются; основная сводка не зелёная при критичных stale/missing данных. |
| R15 | Частичная маржа считается на покрытой части с coverage выручки/операций; общая при неполном cost не объявляется точной. Чековый блок при0receipt свёрнут с одной причиной; никогда не подменяем чек строкой продажи. |
| R16 | Дата остатков и свежесть видны возле складских KPI даже при свёрнутом source panel. Состояние раскрытия источников по умолчанию сохраняется закрытым. |
| R17 | Сохраняются существующие визиты, товарные операции, средняя сумма операции, выручка, тренды, категории и прогноз. Прогноз различает confirmed zero days и отсутствие загрузки, не рисует точные потери на stale stock. |

## Границы и швы

| Модуль | Владеет | Выставляет | Прячет |
|---|---|---|---|
| Langame import | Ежедневный inventory и доказуемая session club binding | существующие sync методы; pure helper разрешения club по tenant/domain/externalClubId | Provider чтения, times/identities, идемпотентность; никаких effects на rewards/ledger |
| Assortment health | Общие правила stocks/demand/prices/OOS/no-sales/turnover/excess/valuation | общий typed engine + loader для DashboardService и ReportsService; summary `assortmentHealth`, те же row sets для существующих reports | Все join/coverage/granularity/exclusion правила |
| Dashboard service | Growth, scoped visits, freshness и интеграция health | существующий GET dashboard/summary с additive `assortmentHealth`, nullable values/reasons/coverage | Чтения фактов, разницу unknown и0 |
| Web | Существующий responsive экран и переходы | постоянные health cards, no-sales selector, report URL adapter | Форматирование и локальные состояния; формулы остаются в backend |

Engine возвращает метрики с value/state/reason/coverage и датой данных; списки OOS/no-sales/turnover используют один и тот же snapshot cutoff, demand21d и store×product. Конкретный TypeScript DTO согласуется исполнителями через interfaces.md до UI подключения. Проверки проходят через pure engine, существующие service boundaries и UI navigation adapter; private-method snapshot tests не заменяют поведенческие случаи.

R13.1: все новые числовые карточки кликабельны, включая стоимость no-sales остатков (тот же список no-sales), оборачиваемость/дни запаса (inventory-turnover) и избыточный запас (inventory-turnover с excess-фильтром). R14.1: причина размещается непосредственно под числом в карточке, не только в отдельной методике. R15.1: каждый partial-показатель показывает coverage рядом со значением; общий badge не заменяет индивидуальное покрытие. Детализация маржи/чеков — реализация R14–R15, не новая функция; security/rollout и UI QA — обязательные условия безопасной приёмки R01–R17 в существующем проекте.

## Реализация и ограничения

- D05 (доказано03→02,R01/R02/R16): идемпотентный inventory day key `snapshotDate=startOfUtcDay(now)` отличается от времени наблюдения. Loader передаёт `observedAt=InventorySnapshot.updatedAt`; engine проверяет оба значения<=asOf, а возраст/фактическую дату берёт из observedAt (fallback snapshotDate для старых adapters). Иначе nightlyUTC23:30 выглядит устаревшим уже через12.5ч при пороге36ч.

- D04 (доказано при02,R04/R09/R15): цена продажи для денежного OOS-риска и оценка замороженного stock — разные величины. Valuation сначала использует per-club purchase configuration либо подтверждённую себестоимость/количество продаж. При отсутствии закупки допускается existing sale/history valuation только с явным basis «оценка по продажной цене», а не как себестоимость/вложенные деньги. Неизвестная цена — null; даты и partial coverage видны. Product.purchasePrice не единственный источник (в live он0).

- D03 (доказано в02,R04/R05/R09): глобальный каталог×все клубы создаёт ложные missingstock строки и искажает покрытие. Loader передаёт engine явный набор `storeProductIds` по доказанной клубной конфигурации/остаткам/продажам в tenant/domain; rows вычисляются только для этих memberships. Shared product может иметь несколько подтверждённых клубов. Полный Cartesian product не является текущим ассортиментом.

- D01 (обнаружено в подготовке01,R03): existing session import выбирает timezone через найденный Store. Новая inferredbinding должна быть отделена от date parsing, чтобы не сдвигать уже сохранённые времена; origin и timestamps остаются прежними. Новый timezone repair не является частью дашборда.
- D02 (обнаружено01,R01/R05/R13/R16): endpoint goods отдаёт текущий склад. Для default dashboard full-day вчерашние продажи не должны отбрасывать сегодняшний реальный снимок. Engine принимает `asOf` для stock и отдельный `demandTo` (default performance `period.to`) для спроса/no-sales; API default использует актуальный stock cutoff, исторический явный `asOf` ограничивает будущее. Ссылки переносят оба якоря, возле KPI видна фактическая дата снимка.

- Existing NestJS/Prisma/Next16/React19; существующие Phosphor, Tailwind и стили дашборда. Не строим новый дизайн/прототип.
- UI проверяется на desktop и узком экране, в пустом/partial/stale/полном состояниях. Keyboard/focus и раскрытия доступны. При tool blocker проверка не объявляется завершённой.
- Одна свежая конфигурация не заменяет старые цены всех остальных клубов. Область pricing всегда tenant+domain+club+product.
- Выпущенный PR201 сохраняется: source domain BALANCE_TOPUP/Reward.storeId может быть NULL; settlement Store только wallet/ledger. Нельзя менять исходные game events, XP, origins, claims или восстанавливать их ради dashboard.
- Live provider credentials не спрашиваем и не печатаем. Нет новых провайдеров/egress/roles/схем без доказанной необходимости. External PILOT/BETA/LIVE unattended остаётся deny. API schedulers OFF.
- Production handoff согласуется с «Спланировать открытый тест»: exact-main Fast/Full, backup/restored-copy, native app rollout, worker grants/canaries, postcheck. Source и фактические live данные проверяются отдельно.
- Невозможную многоклубную связь при отсутствии provider evidence не угадываем: это наблюдаемое неполное покрытие, а не выполнение расчёта любой ценой.

## Вне рамок

Пользовательские требования не откладываются. Новые reward/game возможности, merchant/provider writes, внешняя beta activation, смена хоста и новый UI дизайн не требуются.

## Открытые места

Нет продуктовых вопросов: пользователь одобрил конкретный план. Доступность browser QA и production credentials проверяется по существующим инструментам и handoff; неизвестные значения не заменяются mock-данными в production.

## Покрытие

R01–R17 покрыты одноимёнными строками таблицы и общими правилами границ/приёмки.
