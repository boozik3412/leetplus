# Variant A controller handoff — production acceptance, 24.09.2026

The Variant A release preparation source is admitted and its serving controller is now active. This operation **did not deploy an application release**. The 45–60 minute end-to-end release target remains a hypothesis until a separately approved, controlled comparative pilot.

The admitted target is `88010292246249c94ba66ecbab64e4d51ad84d8c` (Fast `35855405339`, Full `35855405364`, attempt 1; Compose artifact `10747739032`, Docker admission SHA-256 `9a91fd8edd975218041101c57231385463124f6f3b195f9b747bc9bdb5f1ba6d`). A later documentation-only main commit does not change this admitted controller or the deployed application. Five critical main checks remain strict and GitHub Actions-bound.

Under separate preparation and exact-plan production approvals, the operator captured one new encrypted backup, verified it off-host, restored it in an isolated CURRENT191 clone, and passed both-slot corporate/guest boundary, 6GiB resource and cooldown acceptance. The clone's six stopped containers and three empty networks were removed by exact ID; the root and native receipts were archived without disturbing earlier archives. Fresh evidence was bound to the original four-hour backup window, rather than extending the expired prior evidence.

The staged native `control-handoff.sh plan` created operation `63b58306-a711-4a0c-ba8c-07b3d9979e9c`, plan SHA-256 `ede24fc49e34b59c74619fde6cadfc0c78e9204948cfe19a177a9acb6fb93870`. After the separate GO for that exact digest, the existing offline Ed25519 root signed it and one native apply returned `CONTROL_HANDOFF_ACCEPTED` at 06:42:34 UTC. The accepted receipt SHA-256 is `92a9ba1bc5556dc08c5ebb0e702c8c2dae802818135986964fecbf38e84b18f6`. The installed serving controller is `88010292246249c94ba66ecbab64e4d51ad84d8c`, manifest SHA-256 `237cbcba1fbfcc9e58e78aede7b57f599b206f8c02253c43006dd308c04e9c85`, with `preparationEvidenceExpiryEnforced=true` and no pending handoff.

Independent read-only acceptance verified the signed plan/approval/receipt/authority pointer chain and unchanged active record SHA-256 `681838ce8eaad625fa08231d722532e82e595a1c0207b931f12e3fb0406e52bd`: GREEN `f97af35d1f0b54a67f915a37a095336e4f9334f9` generation 10, BLUE `3c3dc4ac4d1a73fdf9a1a6da5427212adfa9af80` hot rollback, data `399876b560b4ac611eae35ee425d99422fb140b9`/CURRENT191. All six original production container IDs, PIDs, start times and restart counts stayed exact and healthy. Four original timers and both gen10 signed grants stayed in place; no application restart or timer stop occurred.

Public API readiness and Web release identity returned HTTP 200 on f97. A fresh, bounded staff-fixture diagnostic proved corporate login HTTP 201, `/auth/me`, four-store `/stores` and guest game-summary HTTP 200, with unsigned and both cross-contour requests HTTP 401. It did not test natural phone/Telegram guest login. CURRENT191 remained the sole primary schema, with 191 completed migrations and none unfinished. Ordinary bonus-ledger worker receipts after handoff passed on the unchanged gen10 grant. The next automatic network-refresh timer/service execution also passed, leaving approved HTTPS/SMTP sets healthy.

The full append-only journal, immutable local plan copy and individual stdout/stderr/exit receipts are under `deploy-evidence/release-preparation-a-handoff-20260923` in the operations workspace; its final index is `FINAL_STATUS_20260924.md`. The older Tickets task was inactive at the coordination checks, and its archived clone was preserved. No provider reward, support ticket, application rollout, worker grant or manual rollback effect belongs to this controller handoff.

Next, use the installed controller for an **independently scoped application release** only after exact-main admission, fresh preparation, a native application plan and its separate plan-bound GO. Record each stage's actual timestamps and wait reasons for the comparative speed pilot. This controller acceptance alone is not evidence that a future release finishes in 45–60 minutes.
